version: '3.8'

services:
  mongodb:
    image: mongo:4.4
    container_name: toothaid-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=toothaid
    restart: unless-stopped

volumes:
  mongodb_data:
